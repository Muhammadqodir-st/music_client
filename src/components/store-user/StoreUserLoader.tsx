export default function StoreUserLoader() {
    return (
        <div className="fixed inset-0 flex flex-col items-center justify-between z-50 py-5">
            <span />
            <p>Loading your account...</p>
        </div>
    )
}